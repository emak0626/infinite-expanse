# Ollama x Docker 連携設定ガイド

Dockerコンテナ内のアプリからホスト側の Ollama に接続できない場合、以下の設定を行ってください。

## 原因
Ollama はデフォルトで `127.0.0.1` (自分自身のみ) で待機しています。Docker コンテナは別のネットワークとして扱われるため、ホスト側の Ollama が「外部からの接続（Docker含む）」を許可するように設定する必要があります。

## 設定手順（Windows）

### 1. 環境変数の設定
1. スタートメニューで「システム環境変数の編集」と入力して開きます。
2. **「環境変数」**ボタンをクリックします。
3. **「ユーザー環境変数」**の**「新規」**をクリックします。
4. 以下の値を入力します：
   - 変数名: `OLLAMA_HOST`
   - 変数値: `0.0.0.0`
5. 全てのウィンドウを「OK」で閉じます。

### 2. Ollama の再起動
1. タスクバー右下のインジケーター（トレイアイコン）から Ollama を探します。
2. アイコンを右クリックし、**「Quit Ollama」**を選択して完全に終了させます。
3. スタートメニューから再度 **Ollama** を起動します。

---
## アプリ側の設定 (.env)
本アプリの `.env` ファイルに以下を追加することで、接続先を明示的に指定することも可能です。
```env
OLLAMA_BASE_URL=http://host.docker.internal:11434/api
```
---
## モデルの保存先をDドライブに変更する（容量確保）
モデルデータ（数GB〜）をDドライブに保存したい場合は、Windowsのシステム環境変数に以下を追加して Ollama を再起動してください：
- 変数名: `OLLAMA_MODELS`
- 変数値: `D:\OllamaModels` (任意のフォルダ)

設定後、コマンドプロンプト等で `ollama pull llama3.1:8b` を実行するとDドライブに保存されるようになります。
