# プロジェクト：Infinite Expanse - 次世代AI駆動型株式分析システム
## ディープリサーチ用技術仕様書・現状レポート

本資料は、現状のシステム構成、開発状況、および当初のコンセプトに基づいた未来の拡張予定をまとめたものです。他のAI（Gemini 1.5 Pro, Search-enabled models等）による深い分析と戦略立案に使用することを目的としています。

---

## 1. システム全体像 (Existing Architecture)

### 1.1 技術スタック
- **Backend**: FastAPI (Python 3.11+) - 非同期I/Oを活用した高パフォーマンスサーバー。
- **Database**: TimescaleDB (PostgreSQL 16) - 時系列データ（株価）に最適化されたデータベース。
- **Frontend**: Vanilla JS / HTML / CSS (Tailwind components) - 軽量かつレスポンシブな設計。
- **Infrastructure**: 自宅サーバー (Windows 11 + Docker Desktop / WSL2)。
- **External APIs**:
    - **auカブコム証券 (Kabu Station API)**: リアルタイム株価、板情報、ランキング取得。
    - **Google Gemini API**: 分析レポート生成（Gemini 2.0 Flash / Pro）。
    - **Google Workspace API**: Drive (成果物保存), Sheets (データ管理), Docs (自動レポート生成)。

### 1.2 コアコンポーネント
- `main.py`: APIエンドポイントの定義、認証、フォールバックロジック。
- `scheduler.py`: 10分ごとの株価取得、日次AI一括スキャン、Workspace同期の制御。
- `kabu_api.py / kabu_com_client.py`: 証券会社APIとの通信クライアント。
- `analyzer_agent.py`: Geminiへのプロンプト注入と構造化データ抽出。
- `repository.py`: SQLAlchemy (async) を用いたデータアクセス層。

---

## 2. 実装済み機能 (Current Status)

### 2.1 監視銘柄管理 (Watchlist Management)
- Web UIからの銘柄検索、追加、削除機能。
- DBでの監視銘柄永続化と、初期シード機能。
- 市場閉場時（土日等）における、DB履歴データへの自動フォールバック表示。

### 2.2 データ収集と蓄積
- TimescaleDBによる分足・日足データの蓄積。
- 市場全体の概況データ（値上がり・値下がり銘柄数等）の記録。
- ローカルDドライブへのデータ永続化（WSL2マウント経由）。

### 2.3 AI分析機能
- 個別銘柄の詳細分析プロンプト自動生成。
- 一括スキャン機能（複数銘柄を同時にスコアリング）。
- 分析結果（スコア、理由、リスク、センチメント）のDB保存。
- NotebookLM向けの一括データエクスポート（Markdown形式）。

### 2.4 堅牢性と安定性
- DB接続プーリングの最適化（pre-ping, recycle）。
- 重大なエラー（Dドライブ未認識等）に対するリトライ処理。
- APIレスポンスのキャッシュ（負荷軽減）。

---

## 3. 解決すべき課題とバグ (Known Issues)
- **データの不整合**: 手動追加時に銘柄名が「Unknown」になる場合がある（同期処理の強化が必要）。
- **インフラの脆さ**: PCスリープ時に外付けDドライブが切断されるとDBが停止する（現在はスリープ無効化で対応中）。
- **UI/UX**: モバイルからの操作性がまだ不十分（情報の密密度が高い）。

---

## 4. 当初コンセプトとのギャップ (Gap Analysis & Roadmap)

当初のPDF資料で掲げられていたが、未実装の高度な機能：

### 4.1 グラフニューラルネットワーク (GNN)
- **目標**: 銘柄を「点」ではなく、サプライチェーンや資本関係という「線」で結んだネットワークとして解析する。
- **欠如**: 現状は個別銘柄の時系列分析が主であり、銘柄を跨いだ相関・波及効果の定量化は未着手。

### 4.2 LTV（ライフタイムバリュー）評価
- **目標**: 顧客LTVの概念を転用し、配当・優待・株価成長を統合した長期トータルリターンの予測数式化。
- **欠如**: Geminiによるテキストベースの評価はあるが、厳密な数式モデルに基づく予測・比較機能は未実装。

### 4.3 マルチモーダル解析
- **目標**: 決算説明会の「音声」や「スライド動画」をGeminiに直接解析させ、経営陣のトーンから非言語情報を抽出。
- **欠如**: 現在はテキストデータ（決算短信等）の解析のみ。

### 4.4 自律型エージェント
- **目標**: 「〇〇セクターの異常な動きを探せ」といった抽象的な指示に対し、自律的にAPI探索を実行するエージェント化。
- **欠如**: 全体のフローは固定されており、柔軟な推論に基づく自律探索機能は未実装。

---

## 5. 分析・戦略エンジンの強化に向けた提案 (AI Strategy)

Deep Research において以下のポイントを掘り下げることを推奨します：

1. **GNNの導入手法**: Python (PyG or DGL) を用いて、日本の製造業サプライチェーンをグラフ化し、Geminiにそのグラフ構造を理解させるためのコンテキスト化手法。
2. **200万トークンの活用**: 10年分の有価証券報告書を全銘柄分Gemini 1.5 Proに読み込ませ、現在の経営戦略との「過去の矛盾」を指摘させるためのプロンプト設計。
3. **モバイル特化UI**: 外出先での「隙間時間」に、AIが推奨する売買ポイントをプッシュ通知や要約で受け取るための、UXデザインの再定義。
4. **エッジでの推論**: 自宅サーバーのGPUリソース（AMD Radeon等）を活用し、リアルタイムで軽量GNNを回しつつ、判断をGeminiに仰ぐ「ハイブリッドAI」の構築。

---
本資料作成日時: 2026年3月15日  
システム開発者: Antigravity (AI Assistant)
